{
  "name": "dentefacil",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix back && npm install --prefix front && npm run build --prefix front",
    "start": "node back/index.js",
    "dev": "concurrently \"npm run dev --prefix back\" \"npm run dev --prefix front\"",
    "install:all": "npm install && npm install --prefix back && npm install --prefix front",
    "lint": "npm run lint --prefix back && npm run lint --prefix front"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
